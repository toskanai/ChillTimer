<Application x:Class="ChillTimer.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>



        <Color x:Key="BgTop">#FFF8F2EC</Color>
        <Color x:Key="BgBottom">#FFF1E7DB</Color>
        <Color x:Key="Card">#FFFFFFFF</Color>
        <Color x:Key="Ink">#FF252525</Color>
        <Color x:Key="InkDim">#99333333</Color>

        <SolidColorBrush x:Key="BrushCard" Color="{StaticResource Card}"/>
        <SolidColorBrush x:Key="BrushText" Color="{StaticResource Ink}"/>
        <SolidColorBrush x:Key="BrushTextDim" Color="{StaticResource InkDim}"/>

        <Style x:Key="ImageButton" TargetType="Button">
            <Setter Property="Width" Value="64"/>
            <Setter Property="Height" Value="64"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                    CornerRadius="20"
                    Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.8"/>
                                <Setter TargetName="bd" Property="Margin" Value="1,2,0,0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ImageButtonSmall" TargetType="Button" BasedOn="{StaticResource ImageButton}">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
        </Style>

    </Application.Resources>
</Application>
